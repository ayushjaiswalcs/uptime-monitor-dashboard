# LOCAL TESTING ONLY. In GitHub Actions the webhook comes from the repo secret
# SLACK_WEBHOOK_URL — do NOT commit a real .env.
#
# Copy this file to .env and paste your webhook to test locally:
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/REPLACE/WITH/YOUR_WEBHOOK
